<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1.0"
        />
        <title>Register Page</title>
    </head>
    <body>
        <h1>
            Register Page
        </h1>
        <% if(locals.successLogin) { %>
            <p>
                <%= successLogin %>
            </p>
        <% } %>
        <%- include("../partials/register-login-form.ejs", {formAction: "/register", buttonText: "Register now!"}) %>
    </body>
</html>
